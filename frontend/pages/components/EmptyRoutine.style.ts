import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    marginTop: 16,
    flexDirection: 'row',
    width: '100%',
  },

  no_routine_message: {
    marginTop: 8,
    flex: 1,
  },
});

export default styles;
