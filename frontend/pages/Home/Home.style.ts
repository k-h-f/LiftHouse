import { StyleSheet } from 'react-native';
import { pageMarginSizes } from '../../shared/themeConfig';

const style = StyleSheet.create({
  Home: {
    margin: pageMarginSizes.all,
  },
});

export default style;
